[phases.setup]
nixPkgs = ["ruby_3_4", "libyaml", "sqlite"]

[phases.install]
cmds = ["bundle install"]

[phases.build]
cmds = [
  "bundle exec rails assets:precompile",
  "bundle exec rails db:create db:migrate"
]

[start]
cmd = "bundle exec rails server -p $PORT -e production"
